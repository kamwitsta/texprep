-- | A somewhat dummy module required by the configuration system (Dyre).
module Main where

import TeXPrep

-- | A somewhat dummy main required by the configiration system (Dyre).
main :: IO ()
main = texprep defaultConfig
-- main = realMain defaultConfig
